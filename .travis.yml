dist: trusty
language: java
jdk: oraclejdk8
cache:
  directories:
  - .autoconf
  - $HOME/.m2 
services:
 - mysql
before_install:
 - mysql -e 'CREATE DATABASE petclinic;'
script: mvn test -Dspring.profiles.active=mysql-travis


notifications:
  email:
    - pabrengar@alum.us.es
    on_success: never
    on_failure: always

